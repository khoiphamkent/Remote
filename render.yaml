services:
  - type: web
    name: remio-like-relay
    runtime: node
    rootDir: relay-server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: ICE_SERVERS_JSON
        value: '[{"urls":"stun:stun.l.google.com:19302"}]'
